<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recibo de Venta</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-gray-100">
    
    <div class="max-w-4xl mx-auto p-8 bg-white rounded-lg shadow-md">
        <div class="flex items-center justify-between mb-8">
            <div class="w-1/4">
                <img src="/img/logo44.png" alt="" class="w-18 h-auto">
            </div>
            <div class="text-center">
                <h1 class="text-xl font-semibold">Recibo de Venta</h1>
            </div>
            <div class="w-1/4"></div>
        </div>
        <div class="bg-gray-200 p-4 rounded-md mb-4">
            <p class="text-lg font-semibold mb-2">Información del cliente</p>
            <p class="mb-2"><span class="font-semibold">Cliente:</span> <%= ventaInfo.ClienteNombre %></p>
            <p class="mb-2"><span class="font-semibold">Fecha de Venta:</span>
                <%= ventaInfo.FechaVenta.toLocaleString('es-ES') %></p>
            <p class="mb-2"><span class="font-semibold">Método de Pago:</span> <%= ventaInfo.MetodoPago %></p>
        </div>

        <table class="w-full border-collapse">
            <thead class="bg-gray-300">
                <tr>
                    <th class="p-2">Producto</th>
                    <th class="p-2">Precio Unitario</th>
                    <th class="p-2">Cantidad</th>
                    <th class="p-2">Total Detalle</th>
                </tr>
            </thead>
            <tbody>
                <% detalles.forEach(detalle => { %>
                <tr class="border-t">
                    <td class="p-2"><%= detalle.NombreProducto %></td>
                    <td class="p-2">$<%= parseFloat(detalle.PrecioUnitario).toFixed(2) %></td>
                    <td class="p-2"><%= detalle.CantidadVendida %></td>
                    <td class="p-2">$<%= (detalle.PrecioUnitario * detalle.CantidadVendida).toFixed(2) %></td>
                </tr>
                <% }); %>
                <tr class="border-t">
                    <td colspan="3" class="p-2 font-semibold">Total</td>
                    <td class="p-2">$<%= totalVentas.toFixed(2) %></td>
                </tr>
                <tr>
                    <td colspan="3" class="p-2 font-semibold">Extra</td>
                    <td class="p-2">$<%= totalExtra.toFixed(2) %></td>
                </tr>
                <tr>
                    <td colspan="3" class="p-2 font-semibold">Total Final</td>
                    <td class="p-2">$<%= (totalVentas + totalExtra).toFixed(2) %></td>
                </tr>
            </tbody>
        </table>
        <div class="mt-8 bg-blue-100 border border-blue-400 text-blue-700 px-4 py-3 rounded relative" role="alert">
            <strong class="font-semibold">¡Gracias por elegirnos!</strong>
            <span class="block sm:inline">Esperamos que disfrutes de la fiesta con nuestras bebidas.</span>
        </div>
    </div>
</body>

</html>
